{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "http://example.com/example.json",
  "type": "object",
  "title": "The service schema",
  "description": "The service schema comprises the entire JSON document.",
  "default": {},
  "examples": [
    {
      "uploadDirectory": "/Users/ddaio/work/LetterMe/fileuploads",
      "port": 8080,
      "defaultAddress": "0.0.0.0"
    }
  ],
  "required": [
    "uploadDirectory",
    "port",
    "defaultAddress"
  ],
  "properties": {
    "uploadDirectory": {
      "$id": "#/properties/uploadDirectory",
      "type": "string",
      "title": "The uploadDirectory schema",
      "description": "An explanation about the purpose of this instance.",
      "default": "",
      "examples": [
        "/Users/ddaio/work/LetterMe/fileuploads"
      ]
    },
    "port": {
      "$id": "#/properties/port",
      "type": "integer",
      "title": "The port schema",
      "description": "An explanation about the purpose of this instance.",
      "default": 80,
      "examples": [
        8080
      ]
    },
    "defaultAddress": {
      "$id": "#/properties/defaultAddress",
      "type": "string",
      "title": "The defaultAddress schema",
      "description": "An explanation about the purpose of this instance.",
      "default": "localhost",
      "examples": [
        "0.0.0.0"
      ]
    }
  },
  "additionalProperties": true
}